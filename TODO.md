# UI Minimalization Task

- [x] Edit `app/globals.css` to enforce minimal light palette and neutralize `.dark` to match.

- [x] Edit `app/components/Layout.tsx` to remove gradients, framer-motion, and dark styling.

- [x] Edit `app/components/Pre_loader.tsx` to replace fancy animated gradients with minimal loader.

- [ ] Edit `app/page.tsx` to remove `bg-gradient-*` and disallowed badge colors.


- [ ] Edit `app/landing/page.tsx` to remove gradients and disallowed colors.
- [ ] Edit `app/components/DatabaseLoading.tsx` and `app/components/DatabaseError.tsx` to remove gradients/animations and disallowed colors.
- [ ] Run lint/build to ensure no TS/Next issues.

